class Shape(object):
    def __init__(sq):
        pass

    def area(same):
        return 0

class Square(Shape):
    def __init__(same, l):
        Shape.__init__(same)
        same.length = l

    def area(same):
        return same.length*same.length

aSquare= Square(0)
print (aSquare.area())